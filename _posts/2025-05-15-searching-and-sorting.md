---
title: 'SearchingAndSorting'
date: 2025-05-15
permalink: /posts/2025/05/searching-and-sorting/
tags:
  - 数据结构与算法
categories:
  - 数据结构与算法
---

# 查找与排序

## 查找

### 顺序查找

#### 普通顺序查找的ASL

#### "哨兵"

待查找元素放在下标0处，**从后往前找**
**优点：无需判断是否越界，效率高**

### 折半查找

***折半查找速度最快***
**规避整数溢出的办法：**```int mid = low +((high - low)/2)```

**折半查找的判定树特点**：
1. 折半查找的判定树一定是**平衡二叉树**，**失败节点有 $n+1$ 个**
2. 折半查找的时间复杂度为$O(log_2n)$

### 二叉搜索树

#### 构造

不同的关键字序列可能得到同款二叉排序树，也可能得到不同款

#### 插入和查找

效率在平衡情况下是$O(log_2n)$，极端情况下退化为$O(n)$

### 平衡二叉树

**平衡因子：左子树高-右子树高**
**使得效率不会退化为$O(n)$**

### 并查集

**根节点指向自己**
**路径压缩：各个节点直接指向根节点**
**合并：按秩合并**

### 散列表（哈希表）

**通过散列函数建立"关键字"与"存储地址"的关系**
**用空间换时间，将时间复杂度降到常数级别**

#### 散列函数

1. 乘法散列函数：归一化后乘上一个常数
2. 模散列函数（除留余法）：**质数因子一般选表长-1**

#### 装填因子

装填因子越大，冲突的可能性越大
装填因子越小，冲突的可能性越小，但是空间利用率越低

#### 处理冲突的方法

1. 拉链法：链表存储
2. 各种探测法
